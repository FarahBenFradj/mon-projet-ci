groups:
- name: alert
  rules:
  - alert: HighCPU
    expr: rate(process_cpu_seconds_total[1m]) > 0.8
    for: 1m
    labels:
      severity: warning
    annotations:
      summary: "CPU élevé détecté"